import React from 'react';
import Login from '../components/Login/Login';

const LoginPage = () => {
    return (
        <section>
            <Login/>
        </section>
    );
};

export default LoginPage;